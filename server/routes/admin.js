/**
 * admin.js is the URL router for adminController.js
 * the admin will manage these roles:
 * 1. manage flights
 * 2. monitor bookings
 */

const express = require('express');
const router = express.Router();

module.exports = router;