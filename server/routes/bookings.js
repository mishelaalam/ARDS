/**
 * bookings.js is the URL router for bookingsController.js
 * the bookings will manage these roles:
 * 1. Create booking
 * 2. View bookings
 * 3. Cancel booking
 */

const express = require('express');
const router = express.Router();

module.exports = router;