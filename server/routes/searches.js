/**
 * searches.js is the URL router for searchesController.js
 * the searches will manage these roles:
 * 1. Save search
 * 2. Delete search
 * 3. Use preferences
 */

const express = require('express');
const router = express.Router();

module.exports = router;